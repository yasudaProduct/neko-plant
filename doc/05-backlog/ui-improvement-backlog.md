# UI改善バックログ

PC(1440px)とスマホ(390px)の実機スクリーンショット、および全画面のコードレビューによるUI改善レビューの記録。

**🔴優先度高(表示崩れ・機能バグ #1〜8)と🟡操作性の大半(#9〜16、下記#14を除く)は対応済みで、develop にマージ済み**(PR #108)。本ファイルは**未対応の項目のみ**を記録する。取り組む際は該当項目を実装し、対応後にこの一覧から削除する。

## 1. 操作性の残り

| # | 内容 | 関連ファイル |
| --- | --- | --- |
| 13 | `/plants/new`(植物単体登録ページ)がどこからもリンクされていない孤立ページ。投稿フロー内(`PhotoPlantSection.tsx`)にも新規登録機能があり重複している。一本化するなら削除し、残すなら `/plants` の検索0件時導線などから参照させる | `src/app/plants/new/page.tsx` |
| 14 | フィード・検索結果のページネーションが前へ/次へのフルリロード型。「もっと見る」ボタンや無限スクロールにすると回遊性が上がる。サーバーコンポーネントの初期取得からクライアント側の追記型フェッチへの再設計が必要な中期タスク | `src/app/page.tsx`, `src/app/plants/page.tsx` |

## 2. 見やすさ(PC)

| # | 内容 | 関連ファイル |
| --- | --- | --- |
| 17 | PCホームの2カラムレイアウトが1440px幅だと中央寄せにならず、フィードとサイドパネルの間に大きな余白ができる | `src/app/page.tsx` |
| 18 | フィルタピル「全て」のアクティブ色がオレンジ。このサイトでオレンジは「情報なし」の警告色として使われており、意味が衝突する | `src/components/np/FilterPills.tsx` |
| 19 | 画像がない植物の検索結果カードが、通常と同じ大きさのグレーのプレースホルダー枠になり、一覧がグレーの壁になりやすい | `src/components/np/PlantResultCard.tsx` |
| 20 | 画像がない投稿がただのグレー矩形になる。植物側で使っている `PhotoPlaceholder` が投稿カード側では使われていない | `src/components/np/PostCard.tsx`, `src/components/np/PostTile.tsx` |
| 21 | 旧スタイルのページが混在している(`np` 系コンポーネントの `rounded-xl border shadow-sm` トーンではなく、旧来の `shadow-lg` や `container mx-auto` を使用) | `src/app/settings/layout.tsx`, `src/app/plants/new/page.tsx`, `src/app/news/page.tsx`, `src/app/contact/page.tsx` |
| 22 | 学名等に使う `text-gray-400` が白背景でWCAG AA未満のコントラスト比。図鑑の並び替えセレクトとフィルタピルの縦位置も揃っていない | 全体的な配色・整列の微調整 |

## 3. コンセプト適合(成長ループ)

| # | 内容 | 関連ファイル |
| --- | --- | --- |
| 23 | 植物詳細に投稿CTAがない。共存実績を調べに来たユーザー(M3)がそのまま投稿(M2)に転換する導線がなく、成長ループが画面上で閉じていない。「情報なし」植物の注意喚起ボックスに添えると特に効果的 | `src/app/plants/[id]/page.tsx` |
| 24 | 「共存実績の多い植物」ランキングに `catCount: 0` の植物が混在し得る(ホームのサイドパネル・植物詳細の分布チャート)。「実績が多い」枠の意味が破綻するため `catCount > 0` のみに絞るべき | `src/app/page.tsx`, `src/app/plants/[id]/page.tsx` |
| 25 | `LoginDialog` の説明文「植物の評価や飼育情報の共有」が現行機能(投稿・いいね)と合っていない。ルートの `metadata.description` の文言もポジティブリスト方針との整合を見直す余地がある | `src/components/LoginDialog.tsx`, `src/app/layout.tsx` |
| 26 | 図鑑のデフォルトフィルタが「全て」で、投稿が少ない現状ではオレンジ(情報なし)の行が大半を占める。デフォルトを「実績あり」にする、または情報なし行の表現を弱めるなどの案 | `src/app/zukan/page.tsx` |
| 27 | サインインページに利用規約・プライバシーポリシーへの同意文言がない | `src/app/(auth-pages)/signin/page.tsx` |

## 4. 副次的価値

| # | 内容 | 関連ファイル |
| --- | --- | --- |
| 28 | モバイルではホームの `aside`(実績ランキング・共存実績の説明)がフィード下に回り、M1(眺めに来た人)がほぼ目にしない位置になる。横スクロールのチップ列などでフィード上部に持ってくる案 | `src/app/page.tsx` |
| 29 | 猫プロフィールで性別・誕生日を入力できる(`PetFormDialog`)のに、一覧では猫種と年齢しか表示されない。また年齢と誕生日の二重入力になっており、誕生日があれば年齢は自動計算できる | `src/app/settings/cats/page.tsx`, `src/app/[aliasId]/ProfileTabs.tsx` |
